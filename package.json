{
  "name": "catalogo-movement",
  "private": true,
  "version": "2.0.0",
  "description": "Catálogo atacado Movement — SSG estático",
  "scripts": {
    "build": "node scripts/build.mjs",
    "images": "python scripts/fetch-images.py",
    "dev": "npx --yes serve dist -p 4173"
  }
}
